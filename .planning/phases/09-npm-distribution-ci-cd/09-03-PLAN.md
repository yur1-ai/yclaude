---
phase: 09-npm-distribution-ci-cd
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - README.md
autonomous: true
requirements:
  - DIST-01

must_haves:
  truths:
    - "README.md exists at project root with install instructions that work as described"
    - "Four badges appear at the top: npm version, CI status, Node version, License"
    - "A hero screenshot reference and feature tour cover all 7 pages listed in CONTEXT.md"
    - "The README communicates the product value prop clearly to a developer arriving from npmjs.com"
  artifacts:
    - path: "README.md"
      provides: "npm storefront — install guide, feature tour, badges"
      contains: "npx yclaude"
  key_links:
    - from: "README.md badges"
      to: "npmjs.com/package/yclaude"
      via: "shields.io npm badge URL"
      pattern: "img.shields.io/npm/v/yclaude"
    - from: "README.md CI badge"
      to: ".github/workflows/ci.yml"
      via: "GitHub Actions badge URL"
      pattern: "github.com.*actions.*workflow"
---

<objective>
Write the polished README.md that serves as the product's storefront on npmjs.com. The README must convince a developer to install in under 30 seconds and give them confidence in what they'll get.

Purpose: First impression on npmjs.com. Most users won't dig into source code — the README is the product pitch.
Output: `README.md` with badges, value prop, install command, feature tour with screenshot placeholders, and all 7 feature pages documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-npm-distribution-ci-cd/09-CONTEXT.md
@.planning/phases/09-npm-distribution-ci-cd/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md — product storefront</name>
  <files>README.md</files>
  <action>
    Create `README.md` at the project root. Follow the structure locked in CONTEXT.md exactly:

    **Structure (in order):**

    1. **Title + one-liner value prop** — punchy, developer-focused. "yclaude" and "Why, Claude?!" brand voice. The one-liner goes right under the title before anything else.

    2. **Four badges on one line:**
       - npm version: `[![npm version](https://img.shields.io/npm/v/yclaude.svg)](https://www.npmjs.com/package/yclaude)`
       - CI status: `[![CI](https://github.com/ishevtsov/yclaude/actions/workflows/ci.yml/badge.svg)](https://github.com/ishevtsov/yclaude/actions/workflows/ci.yml)` — use the actual GitHub repo URL (check git remote: `git remote get-url origin`)
       - Node version: `[![Node >=24](https://img.shields.io/node/v/yclaude.svg)](https://www.npmjs.com/package/yclaude)`
       - License: `[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)`

    3. **Hero screenshot** — use a placeholder comment: `<!-- screenshot: Overview dashboard -->\n![yclaude Overview](docs/screenshots/overview.png)`. The image won't render until screenshots are added, but the structure is in place.

    4. **Install section** with a single command:
       ```
       npx yclaude
       ```
       Include a brief note: works without installing — add `-g` for permanent install if preferred.

    5. **Feature tour** — one short paragraph per feature (2-3 sentences max) paired with a screenshot placeholder. Cover these 7 features in this order per CONTEXT.md:
       - Overview (spend summary, cache efficiency, activity heatmap)
       - Cost over time chart (day/week/month/24h views)
       - Model breakdown (donut chart + sortable table)
       - Session explorer (paginated list, filters, subagent badges)
       - Session detail (per-turn token breakdown, cost timeline)
       - Activity heatmap (GitHub-style, daily intensity, 90th percentile annotations)
       - Dark mode (system-aware, manual toggle, persists across sessions)

       Screenshot placeholder format: `<!-- screenshot: [feature name] -->\n![yclaude [feature]](docs/screenshots/[feature-slug].png)`

    6. **Options section** — document the CLI flags:
       ```
       yclaude [options]

       Options:
         -d, --dir <path>     Custom data directory (default: ~/.claude)
         -p, --port <number>  Port number (default: 3000)
         --no-open            Do not open browser automatically
         --debug              Enable debug logging
         -v, --version        Output the version number
         -h, --help           Display help
       ```

    7. **How it works** — 3-4 bullet points: reads from `~/.claude/projects/**/*.jsonl`, never sends data anywhere, runs entirely locally on 127.0.0.1.

    8. **Requirements** — Node.js >=24.

    9. **License** — MIT.

    **Tone:** Use the "Why, Claude?!" personality voice. Dry humor is fine (e.g., "Find out why Claude keeps spending your tokens on that one recursive function"). Keep the main copy professional but add one or two personality touches. The README is a storefront, not a comedy show.

    **Get the GitHub repo URL for the CI badge:** Run `git remote get-url origin` to get the actual repository URL and use it in the CI badge. If the URL is SSH format (`git@github.com:user/repo.git`), convert to HTTPS format for the badge.

    **License field in package.json:** Check if `package.json` has a `"license"` field. If not, add `"license": "MIT"` and create a `LICENSE` file with the MIT license text (use the standard MIT template with the current year and "ishevtsov" as the copyright holder — or get the author name from `git config user.name`).
  </action>
  <verify>
    <automated>grep -c "npx yclaude\|shields.io\|screenshot\|Feature" /Users/ishevtsov/ai-projects/yclaude/README.md</automated>
  </verify>
  <done>
    - `README.md` exists at project root
    - Contains all 4 badges (npm, CI, Node, License)
    - Contains install command (`npx yclaude`)
    - Contains feature tour covering all 7 features from CONTEXT.md
    - Contains CLI options documentation
    - Screenshot placeholders are present for hero + all 7 features
    - `package.json` has `"license": "MIT"` field (add if missing)
    - `LICENSE` file exists with MIT license text (create if missing)
  </done>
</task>

</tasks>

<verification>
After task completes:
1. README.md exists: `ls -la README.md`
2. Contains badges: `grep "shields.io" README.md | wc -l` (expect 3+)
3. Contains install command: `grep "npx yclaude" README.md`
4. Contains all feature sections: `grep -i "overview\|cost.*time\|model\|session\|heatmap\|dark mode" README.md | wc -l` (expect 7+)
5. Contains CLI options: `grep -- "--no-open\|--port\|--dir" README.md | wc -l`
</verification>

<success_criteria>
- README.md is a complete product storefront that would convince a developer to try yclaude
- All 7 feature areas documented with screenshot placeholders
- Badges correctly formatted (will render on npmjs.com once published)
- Install instructions are the first thing a user sees after the title
- License is declared in both README and package.json
</success_criteria>

<output>
After completion, create `.planning/phases/09-npm-distribution-ci-cd/09-03-SUMMARY.md`
</output>
