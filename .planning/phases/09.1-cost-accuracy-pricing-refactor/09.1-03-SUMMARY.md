---
phase: 09.1-cost-accuracy-pricing-refactor
plan: 03
subsystem: ui
tags: [react, tooltip, css-only, tailwind, dark-mode]

# Dependency graph
requires:
  - phase: 09.1-cost-accuracy-pricing-refactor
    plan: 01
    provides: "7-tier pricing architecture with metadata exports (PRICING_LAST_UPDATED, PRICING_SOURCE)"
provides:
  - "CostInfoTooltip reusable component with CSS-only hover pattern"
  - "StatCard.labelSuffix prop for appending ReactNode after label text"
  - "SortableTable Column.label widened to React.ReactNode"
  - "Tooltip explaining API-based pricing on Overview, Models, and Sessions pages"
affects: [ui, dashboard, cost-display]

# Tech tracking
tech-stack:
  added: []
  patterns: ["CSS-only group-hover tooltip with inline SVG icon", "labelSuffix prop pattern for composable StatCard"]

key-files:
  created:
    - web/src/components/CostInfoTooltip.tsx
  modified:
    - web/src/components/StatCard.tsx
    - web/src/components/SortableTable.tsx
    - web/src/pages/Overview.tsx
    - web/src/pages/Models.tsx
    - web/src/pages/Sessions.tsx

key-decisions:
  - "CostInfoTooltip uses inline SVG info-circle icon (w-3.5 h-3.5), not Unicode -- consistent cross-browser rendering"
  - "Tooltip width w-64 with text-left wrapping -- tooltip text too long for whitespace-nowrap"
  - "Models donut chart: added 'Spend by model' heading with tooltip above chart instead of modifying SVG text element (SVG cannot contain React components)"
  - "StatCard labelSuffix as optional ReactNode -- non-breaking change for all existing callers"
  - "SortableTable Column.label widened from string to React.ReactNode -- safe because label is only used in JSX rendering, never for string operations or React keys"

patterns-established:
  - "CostInfoTooltip: reusable info icon + tooltip for cost disclaimers; import and render inline"
  - "StatCard.labelSuffix: composable slot after label text for icons/badges"

requirements-completed: [9.1-01, 9.1-04]

# Metrics
duration: 4min
completed: 2026-03-04
---

# Phase 9.1 Plan 03: Cost Info Tooltips Summary

**Reusable CostInfoTooltip component with CSS-only hover pattern wired into Overview StatCards, Models chart heading + table, and Sessions table header**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-04T14:27:49Z
- **Completed:** 2026-03-04T14:31:34Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments
- Created CostInfoTooltip component with inline SVG info icon and CSS-only group-hover tooltip
- Added labelSuffix prop to StatCard enabling composable icon/badge placement after label text
- Widened SortableTable Column.label from string to React.ReactNode for JSX-based column headers
- Wired tooltips into Overview (2 cost StatCards), Models (chart heading + Cost column), and Sessions (Cost column header)
- Tooltip text exactly matches user specification: "Based on API token pricing. If you're on Claude Pro or Max, your actual spend is your subscription fee -- not these numbers."
- All existing $X.XX est. format unchanged throughout the application

## Task Commits

Each task was committed atomically:

1. **Task 1: Create CostInfoTooltip and update StatCard/SortableTable** - `31722ea` (feat)
2. **Task 2: Wire CostInfoTooltip into Overview, Models, and Sessions pages** - `a7a26a3` (feat)

## Files Created/Modified
- `web/src/components/CostInfoTooltip.tsx` - Reusable info icon + CSS-only tooltip component (NEW)
- `web/src/components/StatCard.tsx` - Added optional labelSuffix prop
- `web/src/components/SortableTable.tsx` - Widened Column.label type to React.ReactNode
- `web/src/pages/Overview.tsx` - CostInfoTooltip on both cost StatCard labels
- `web/src/pages/Models.tsx` - CostInfoTooltip on "Spend by model" heading and Cost column header
- `web/src/pages/Sessions.tsx` - CostInfoTooltip on Cost column header

## Decisions Made
- Used inline SVG info-circle icon (w-3.5 h-3.5) instead of Unicode -- consistent cross-browser rendering
- Tooltip uses w-64 text-left wrapping instead of whitespace-nowrap -- tooltip text is too long for a single line
- Models page: added "Spend by model" heading with tooltip above donut chart rather than trying to embed React component inside SVG `<text>` element (which is impossible)
- Icon color: text-slate-400 dark:text-[#6e7681] -- slightly dimmer than label text to indicate supplementary information
- Tooltip background: bg-slate-800 dark:bg-slate-700 -- consistent with existing token breakdown tooltip pattern

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- Phase 9.1 plan 03 is the final plan in this phase
- All cost accuracy improvements complete: 7-tier pricing (plan 01), unknown model detection (plan 02), info tooltips (plan 03)
- Dashboard now transparently communicates that cost estimates are API-based, not subscription-based

## Self-Check: PASSED

All 7 files verified present. Both task commits (31722ea, a7a26a3) verified in git log.

---
*Phase: 09.1-cost-accuracy-pricing-refactor*
*Completed: 2026-03-04*
