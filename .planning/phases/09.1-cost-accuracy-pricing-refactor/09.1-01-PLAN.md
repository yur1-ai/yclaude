---
phase: 09.1-cost-accuracy-pricing-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cost/pricing.ts
  - src/cost/__tests__/pricing.test.ts
autonomous: true
requirements:
  - 9.1-02
  - 9.1-04

must_haves:
  truths:
    - "pricing.ts has named tier constants (not inline objects per model)"
    - "MODEL_PRICING entries reference shared tier objects"
    - "PRICING_LAST_UPDATED and PRICING_SOURCE are exported constants"
    - "engine.ts imports continue to work unchanged"
    - "All 137+ existing tests still pass"
    - "New tests verify tier deduplication and metadata exports"
  artifacts:
    - path: "src/cost/pricing.ts"
      provides: "Tier definitions + model-ID lookup + metadata exports"
      exports: ["ModelPricing", "KnownModel", "MODEL_PRICING", "PRICING_LAST_UPDATED", "PRICING_SOURCE"]
      contains: "TIER_OPUS"
    - path: "src/cost/__tests__/pricing.test.ts"
      provides: "Tests for tier structure, metadata exports, and alias correctness"
      min_lines: 90
  key_links:
    - from: "src/cost/engine.ts"
      to: "src/cost/pricing.ts"
      via: "import { MODEL_PRICING } and import type { ModelPricing }"
      pattern: "MODEL_PRICING.*ModelPricing"
---

<objective>
Refactor pricing.ts from inline price objects per model ID to a tier-reference architecture with named tier constants, and add metadata exports (PRICING_LAST_UPDATED, PRICING_SOURCE). Expand pricing tests to cover the new structure.

Purpose: Eliminates 19 duplicated inline objects (only 7 distinct tiers), makes pricing auditable, and adds maintainability metadata. This is the structural foundation that must land before any UI work.
Output: Refactored pricing.ts with identical exports, expanded pricing.test.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-cost-accuracy-pricing-refactor/09.1-CONTEXT.md
@.planning/phases/09.1-cost-accuracy-pricing-refactor/09.1-RESEARCH.md
@src/cost/pricing.ts
@src/cost/engine.ts
@src/cost/__tests__/pricing.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/cost/pricing.ts (CURRENT — to be refactored):
```typescript
export interface ModelPricing {
  inputPerMTok: number;
  outputPerMTok: number;
  cacheWrite5mPerMTok: number;
  cacheWrite1hPerMTok: number;
  cacheReadPerMTok: number;
}
export const MODEL_PRICING = { ... } satisfies Record<string, ModelPricing>;
export type KnownModel = keyof typeof MODEL_PRICING;
```

From src/cost/engine.ts (CONSUMER — must not break):
```typescript
import { MODEL_PRICING } from './pricing.js';
import type { ModelPricing } from './pricing.js';
// Line 32: const pricing = (MODEL_PRICING as Record<string, ModelPricing | undefined>)[event.model];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refactor pricing.ts to tier-reference architecture</name>
  <files>src/cost/pricing.ts</files>
  <behavior>
    - Test: PRICING_LAST_UPDATED is exported and equals '2026-02-28'
    - Test: PRICING_SOURCE is exported and is a non-empty URL string
    - Test: MODEL_PRICING['claude-opus-4-6'] and MODEL_PRICING['claude-opus-4-5'] reference the same tier (===)
    - Test: MODEL_PRICING['claude-opus-4-1'] and MODEL_PRICING['claude-opus-4-0'] reference the same tier (===)
    - Test: All 19 existing model IDs still present with correct numeric values
    - Test: Cache multiplier invariant holds (5m = 1.25x input, 1h = 2.0x input, read = 0.1x input) for every tier
  </behavior>
  <action>
  Restructure pricing.ts into two logical sections within the same file. Per user decision: keep single file, keep ModelPricing interface and KnownModel type in same module.

  Section 1 — Metadata + Tier Definitions:
  - Add `export const PRICING_LAST_UPDATED = '2026-02-28';`
  - Add `export const PRICING_SOURCE = 'https://docs.anthropic.com/en/docs/about-claude/models';`
  - Define 7 named tier constants (not exported — internal to module):
    - TIER_OPUS_PREMIUM: inputPerMTok=15.0, outputPerMTok=75.0 (Opus 4.0, 4.1, 3-opus)
    - TIER_OPUS: inputPerMTok=5.0, outputPerMTok=25.0 (Opus 4.5, 4.6)
    - TIER_SONNET: inputPerMTok=3.0, outputPerMTok=15.0 (Sonnet 4.0, 4.5, 4.6, 3.7)
    - TIER_HAIKU_45: inputPerMTok=1.0, outputPerMTok=5.0
    - TIER_HAIKU_35: inputPerMTok=0.8, outputPerMTok=4.0
    - TIER_HAIKU_3: inputPerMTok=0.25, outputPerMTok=1.25
    - TIER_SONNET_3: inputPerMTok=3.0, outputPerMTok=15.0 (kept separate from TIER_SONNET for generational clarity per RESEARCH.md)
  - Each tier includes all 5 cache fields computed from multiplier rules.

  Section 2 — Model ID to Tier Mapping:
  - MODEL_PRICING entries now reference tier constants instead of inline objects.
  - Preserve `satisfies Record<string, ModelPricing>` pattern.
  - All 19 model IDs remain. Comments group by model family.
  - Keep existing source/date header comment, update to mention the tier architecture.

  CRITICAL: All existing exports must remain: MODEL_PRICING, ModelPricing, KnownModel. The new PRICING_LAST_UPDATED and PRICING_SOURCE are additive only. engine.ts imports must not break.
  </action>
  <verify>
    <automated>cd /Users/ishevtsov/ai-projects/yclaude && npx vitest run src/cost/__tests__/pricing.test.ts src/cost/__tests__/engine.test.ts</automated>
  </verify>
  <done>pricing.ts uses 7 named tier constants referenced by 19 model entries; PRICING_LAST_UPDATED and PRICING_SOURCE exported; all engine.test.ts and pricing.test.ts pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand pricing tests for tier architecture and metadata</name>
  <files>src/cost/__tests__/pricing.test.ts</files>
  <behavior>
    - Test: PRICING_LAST_UPDATED is a string matching YYYY-MM-DD format
    - Test: PRICING_SOURCE is a string starting with 'https://'
    - Test: Tier deduplication — aliases share object identity (MODEL_PRICING['claude-opus-4-5'] === MODEL_PRICING['claude-opus-4-5-20251101'])
    - Test: Every tier's cache multipliers are consistent (5m = 1.25x, 1h = 2.0x, read = 0.1x of inputPerMTok)
    - Test: All existing 19 model IDs still defined with correct prices (existing tests preserved)
  </behavior>
  <action>
  Expand the existing pricing.test.ts. Keep all 5 existing test cases unchanged. Add new describe blocks:

  1. `describe('Pricing metadata')`:
     - PRICING_LAST_UPDATED matches /^\d{4}-\d{2}-\d{2}$/ pattern
     - PRICING_SOURCE starts with 'https://'

  2. `describe('Tier reference deduplication')`:
     - Verify alias pairs share identity (===):
       - claude-opus-4-5 === claude-opus-4-5-20251101
       - claude-haiku-4-5 === claude-haiku-4-5-20251001
       - claude-sonnet-4-5 === claude-sonnet-4-5-20250929
       - claude-opus-4-1 === claude-opus-4-1-20250805
       - claude-sonnet-4-0 === claude-sonnet-4-20250514
       - claude-opus-4-0 === claude-opus-4-20250514

  3. `describe('Cache multiplier invariants')`:
     - For every entry in MODEL_PRICING, verify:
       - cacheWrite5mPerMTok === inputPerMTok * 1.25
       - cacheWrite1hPerMTok === inputPerMTok * 2.0
       - cacheReadPerMTok === inputPerMTok * 0.1
     - Use toBeCloseTo for floating point safety.

  Import PRICING_LAST_UPDATED and PRICING_SOURCE from '../pricing.js'.
  </action>
  <verify>
    <automated>cd /Users/ishevtsov/ai-projects/yclaude && npx vitest run src/cost/__tests__/pricing.test.ts</automated>
  </verify>
  <done>pricing.test.ts has 10+ test cases covering metadata exports, tier deduplication, and cache multiplier invariants; all pass</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — full suite passes (137+ tests)
2. `npm run typecheck` — no type errors
3. engine.ts unchanged — only pricing.ts and its test modified
4. `grep -c 'export' src/cost/pricing.ts` shows MODEL_PRICING, ModelPricing, KnownModel, PRICING_LAST_UPDATED, PRICING_SOURCE
</verification>

<success_criteria>
- pricing.ts has 7 named tier constants and 19 model IDs referencing them
- PRICING_LAST_UPDATED = '2026-02-28' and PRICING_SOURCE exported
- engine.ts untouched, all its tests pass
- pricing.test.ts covers metadata, deduplication, and multiplier invariants
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-cost-accuracy-pricing-refactor/09.1-01-SUMMARY.md`
</output>
