---
phase: 13-multi-provider-api-dashboard
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - web/src/components/Layout.tsx
  - web/src/components/ProviderTabs.tsx
  - web/src/pages/Sessions.tsx
  - web/src/pages/Chats.tsx
  - web/src/pages/Models.tsx
autonomous: true
requirements: [PROV-03, CROSS-02]

must_haves:
  truths:
    - "User sees provider tabs [All] [Claude] [Cursor] below the yclaude header when 2+ providers are loaded"
    - "User sees no tabs when only 1 provider is loaded (identical to v1.1)"
    - "Switching tab stays on current page and preserves date range"
    - "Sessions page shows provider badge per row in All-view"
    - "Chats page shows provider badge per row in All-view"
    - "Models page shows provider column with badge per row in All-view"
    - "Pages not supported by a provider show as disabled/grayed-out in nav"
    - "Session type filter (All / Composer / Edit) appears on Sessions page"
  artifacts:
    - path: "web/src/components/ProviderTabs.tsx"
      provides: "Provider tab switching component"
      exports: ["ProviderTabs"]
      min_lines: 20
    - path: "web/src/components/Layout.tsx"
      provides: "Sidebar with provider tabs and provider-aware nav items"
      contains: "ProviderTabs"
    - path: "web/src/pages/Sessions.tsx"
      provides: "Sessions page with provider badge and session type filter"
      contains: "ProviderBadge"
    - path: "web/src/pages/Chats.tsx"
      provides: "Chats page with provider badge"
      contains: "ProviderBadge"
    - path: "web/src/pages/Models.tsx"
      provides: "Models page with provider column in All-view"
      contains: "ProviderBadge"
  key_links:
    - from: "web/src/components/ProviderTabs.tsx"
      to: "web/src/store/useProviderStore.ts"
      via: "reads provider/providers, calls setProvider on click"
      pattern: "useProviderStore"
    - from: "web/src/components/Layout.tsx"
      to: "web/src/components/ProviderTabs.tsx"
      via: "renders ProviderTabs between header and nav"
      pattern: "ProviderTabs"
    - from: "web/src/pages/Sessions.tsx"
      to: "web/src/store/useProviderStore.ts"
      via: "reads provider to conditionally show provider badge column and session type filter"
      pattern: "useProviderStore"
---

<objective>
Add provider tab navigation in the sidebar, provider badges on list pages (Sessions, Chats, Models), provider-aware nav item disabling, and session type filter on the Sessions page.

Purpose: This gives users the ability to switch between providers and see provider-specific data in all list views. Combined with the API filtering from Plan 01, switching a tab filters all data across the application.

Output: Working sidebar tabs, provider badges on all list pages, session type filter dropdown, disabled nav items for unsupported provider features.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-multi-provider-api-dashboard/13-CONTEXT.md
@.planning/phases/13-multi-provider-api-dashboard/13-RESEARCH.md
@.planning/phases/13-multi-provider-api-dashboard/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (will exist when this plan executes) -->

From web/src/store/useProviderStore.ts:
```typescript
type ProviderFilter = ProviderId | 'all';
interface ProviderState {
  provider: ProviderFilter;
  providers: Array<{ id: ProviderId; name: string; eventCount: number }>;
  setProvider: (p: ProviderFilter) => void;
  setProviders: (list: Array<{ id: ProviderId; name: string; eventCount: number }>) => void;
}
export const useProviderStore = create<ProviderState>(...);
```

From web/src/lib/providers.ts:
```typescript
export const PROVIDER_COLORS: Record<ProviderId | 'all', { light: string; dark: string }>;
export const PROVIDER_NAMES: Record<ProviderId, string>;
export type ProviderFilter = ProviderId | 'all';
```

From web/src/components/ProviderBadge.tsx:
```typescript
export function ProviderBadge({ provider }: { provider: ProviderId }): JSX.Element;
```

From web/src/hooks/useConfig.ts:
```typescript
interface AppConfig {
  showMessages: boolean;
  providers?: Array<{ id: string; name: string; eventCount: number }>;
}
```

From web/src/components/Layout.tsx (current):
```typescript
const navItems = [
  { to: '/', label: 'Overview', end: true },
  { to: '/models', label: 'Models' },
  { to: '/projects', label: 'Projects' },
  { to: '/sessions', label: 'Sessions' },
];
// Chats rendered separately with showMessages gating
```

From web/src/pages/Sessions.tsx (current):
```typescript
// Uses useSessions hook, SortableTable, columns array with SessionRow type
// Existing filters: project (from useProjects), branch (from useBranches)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProviderTabs component + Layout sidebar integration + provider-aware nav</name>
  <files>web/src/components/ProviderTabs.tsx, web/src/components/Layout.tsx</files>
  <action>
    1. Create web/src/components/ProviderTabs.tsx:
       - Import useProviderStore, PROVIDER_COLORS
       - Read `{ provider, setProvider, providers }` from store
       - Gate: `if (providers.length < 2) return null;` -- single-provider users see NO tabs (user decision: identical to v1.1)
       - Build tabs array: `[{ id: 'all', label: 'All' }, ...providers.map(p => ({ id: p.id, label: p.name }))]`
       - For each tab, render a button with colored left border or bottom indicator using PROVIDER_COLORS
       - Active state: `bg-slate-100 text-slate-900 dark:bg-[#21262d] dark:text-[#e6edf3]` (matches existing nav active style)
       - Inactive state: `text-slate-500 hover:text-slate-700 dark:text-[#8b949e] dark:hover:text-[#e6edf3]`
       - Tab labels are name only -- no counts or costs inline (user decision)
       - Container: `px-3 py-2 border-b border-slate-200 dark:border-[#30363d] flex gap-1`
       - Each tab button: `px-3 py-1 rounded text-xs font-medium transition-colors`

    2. Modify web/src/components/Layout.tsx:
       a. Import ProviderTabs, useProviderStore, useConfig, and useEffect
       b. In Layout component, call useConfig() and useProviderStore
       c. Add useEffect to populate provider store from config:
          ```typescript
          const { data: config } = useConfig();
          const { setProviders } = useProviderStore();
          useEffect(() => {
            if (config?.providers) setProviders(config.providers);
          }, [config?.providers, setProviders]);
          ```
       d. Render `<ProviderTabs />` in the sidebar between the "yclaude" header div and the nav element
       e. Make nav items provider-aware: define which pages are supported per provider:
          ```typescript
          const providerPages: Record<string, string[]> = {
            claude: ['/', '/models', '/projects', '/sessions', '/chats'],
            cursor: ['/', '/models', '/sessions', '/chats'],
            all: ['/', '/models', '/projects', '/sessions', '/chats'],
          };
          ```
          Projects is NOT available for Cursor (Cursor sessions can have cwd but the "Projects" aggregation view is Claude-specific in behavior). When a nav item's `to` is not in the current provider's page list, render it with `opacity-40 pointer-events-none` class and a `tabIndex={-1}` to make it visually grayed out but still visible (user decision: disabled/grayed-out, not hidden).
       f. Keep the existing navItems array structure. For each NavLink, check if the route is supported for the current provider. Apply the disabled styling conditionally.
  </action>
  <verify>
    <automated>cd /Users/ishevtsov/ai-projects/yclaude/web && npx tsc --noEmit</automated>
  </verify>
  <done>ProviderTabs renders tabs when 2+ providers loaded, returns null for single provider. Layout renders ProviderTabs between header and nav. Nav items are grayed out when unsupported by current provider. Switching tabs updates the provider store (which triggers all hooks to refetch via queryKey change from Plan 01).</done>
</task>

<task type="auto">
  <name>Task 2: Provider badges on Sessions/Chats/Models + session type filter</name>
  <files>web/src/pages/Sessions.tsx, web/src/pages/Chats.tsx, web/src/pages/Models.tsx</files>
  <action>
    1. Modify web/src/pages/Sessions.tsx:
       a. Import ProviderBadge from components, useProviderStore from store
       b. Read `const { provider } = useProviderStore();`
       c. Add a provider badge column to the columns array, visible when provider is 'all':
          - Insert as the first or second column: `{ key: 'provider', label: 'Provider', sortable: true, render: (row) => <ProviderBadge provider={row.provider} /> }`
          - The useSessions hook should already pass ?provider= to the API (from Plan 01). The API returns a `provider` field on session rows... Actually, the sessions API currently does NOT return a provider field per session row. The session grouping logic groups by sessionId, and events within a session all have the same provider. We need to add `provider` to the SessionRow response in api.ts. BUT api.ts is Plan 01's territory. Instead, since each session's events come from one provider, the first event's provider should be captured. Let's check: the sessions endpoint in api.ts builds SessionRow but does not include provider. We need to add it.
          - IMPORTANT: The API modification to include `provider` in session/chat list responses should have been part of Plan 01's api.ts changes. If it wasn't, add `provider: sorted[0]?.provider ?? 'claude'` to the SessionRow construction in api.ts AND to the corresponding SessionRow interface in useSessions hook. This is a minimal addition to api.ts that complements the provider filtering already done in Plan 01.
       d. Also update the SessionRow type in useSessions.ts hook to include `provider: string` if not already present.
       e. Add session type filter dropdown alongside the existing Branch filter:
          - Add a `sessionType` state: `const [sessionType, setSessionType] = useState<'all' | 'composer' | 'edit'>('all');`
          - Render a `<select>` dropdown with options: All, Composer, Edit
          - Only show this dropdown when provider is 'all' or 'cursor' (Claude sessions don't have sessionType)
          - Pass sessionType to useSessions hook, which passes it as ?sessionType= query param
          - In api.ts /sessions endpoint, add sessionType filtering: `if (sessionTypeParam) events = events.filter(e => e.sessionType === sessionTypeParam || e.sessionType === undefined);` -- events without sessionType (Claude) pass through when filter is active, so All-view shows Cursor filtered + all Claude sessions. Note: this api.ts change is a small extension. If Plan 01 didn't add sessionType query param support, add it here.
       f. Conditionally show/hide the provider column based on whether provider is 'all' (show) or specific (hide, since all rows would be same provider).

    2. Modify web/src/pages/Chats.tsx:
       a. Import ProviderBadge, useProviderStore
       b. Add provider badge to chat list items (the Chats page already has a `provider` field in ChatItem from the API -- see api.ts line 907: `provider: sorted[0]?.provider ?? 'claude'`)
       c. Add provider badge in the chat card or list rendering, next to the model name or as a separate visual element
       d. Only show provider badge when provider is 'all' (mixed list)

    3. Modify web/src/pages/Models.tsx:
       a. Import ProviderBadge, useProviderStore
       b. Add provider column to the models table when provider is 'all'
       c. The models API endpoint groups by model name, so in All-view the same model (e.g., "claude-sonnet-4-20250514") could appear from multiple providers. But since models are currently grouped by model string without provider context, we need a different approach: in All-view, add the provider as part of the grouping key (model+provider) so "Sonnet in Claude" and "Sonnet in Cursor" appear as separate rows. This requires an API change to /models: when no ?provider= filter, group by model+provider and include provider in each row.
       d. Alternatively (simpler): since model names are typically different across providers (Claude uses claude-* models, Cursor uses gpt-4o, claude-* etc.), the existing model grouping may already separate them naturally. The provider badge just needs to show which provider a model row came from. Add a `provider` field to the models API response rows (the dominant provider for that model). In api.ts /models endpoint, track the dominant provider for each model grouping key. This is a small api.ts extension.
       e. Show provider badge column only when provider is 'all'.
  </action>
  <verify>
    <automated>cd /Users/ishevtsov/ai-projects/yclaude && npx vitest run src/server/__tests__/api.test.ts src/server/__tests__/api-sessions.test.ts src/server/__tests__/api-provider-filter.test.ts && cd web && npx tsc --noEmit</automated>
  </verify>
  <done>Sessions page shows provider badge column in All-view, session type filter dropdown appears for Cursor/All views. Chats page shows provider badge per row in All-view. Models page shows provider column with badge in All-view. Provider column hidden in single-provider view. All tests pass. TypeScript clean.</done>
</task>

</tasks>

<verification>
1. TypeScript: `cd web && npx tsc --noEmit` -- zero errors
2. Full test suite: `npx vitest run` -- all tests pass
3. Visual: With 2+ providers, tabs appear below "yclaude" header. With 1 provider, no tabs visible.
4. Navigation: Click Cursor tab, switch to Sessions -- sessions shown are Cursor-only. Click All tab -- all sessions shown with provider badges.
5. Models All-view has provider column with badges. Single-provider view has no provider column.
6. Session type filter on Sessions page: selecting Composer filters to Cursor composer sessions + all Claude sessions.
7. Projects nav item grayed out when Cursor tab selected.
</verification>

<success_criteria>
- ProviderTabs renders [All] [Claude] [Cursor] when 2+ providers loaded
- Single-provider shows no tabs (identical to v1.1)
- Switching tabs stays on current page, date range preserved
- Projects nav item grayed out for Cursor provider
- Sessions, Chats, Models show provider badge column in All-view
- Provider badge column hidden in single-provider view
- Session type filter dropdown on Sessions page (visible for Cursor/All)
- All tests pass, TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/13-multi-provider-api-dashboard/13-02-SUMMARY.md`
</output>
