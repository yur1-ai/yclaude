---
phase: 03-server-cli-app-shell
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsup.config.ts
  - src/server/cli.ts
  - src/server/server.ts
  - src/server/routes/api.ts
  - src/server/__tests__/cli.test.ts
  - src/server/__tests__/server.test.ts
  - src/server/__tests__/api.test.ts
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CORE-04

must_haves:
  truths:
    - "Server binds exclusively to 127.0.0.1 — network probes from other interfaces get no response"
    - "CSP header blocks all external requests: defaultSrc 'none', connectSrc 'self' only"
    - "GET /api/v1/summary returns JSON with totalCost, totalTokens, eventCount"
    - "CLI --dir, --port, and --no-open flags parse and apply correctly"
    - "All tests pass: npm test"
  artifacts:
    - path: "src/server/cli.ts"
      provides: "CLI entry point with shebang, Commander flag parsing, server startup"
      exports: ["(executable shebang entry — no named exports)"]
    - path: "src/server/server.ts"
      provides: "Hono app factory with CSP middleware, API routes, static serving"
      exports: ["createApp", "AppState"]
    - path: "src/server/routes/api.ts"
      provides: "GET /api/v1/summary route handler"
      exports: ["apiRoutes"]
    - path: "src/server/__tests__/server.test.ts"
      provides: "Tests: 127.0.0.1 binding, CSP headers, route ordering"
    - path: "src/server/__tests__/api.test.ts"
      provides: "Tests: /api/v1/summary response shape"
    - path: "src/server/__tests__/cli.test.ts"
      provides: "Tests: Commander option parsing, --no-open default"
  key_links:
    - from: "src/server/cli.ts"
      to: "src/index.ts"
      via: "import { parseAll } from '../index.js'"
      pattern: "parseAll"
    - from: "src/server/cli.ts"
      to: "src/server/server.ts"
      via: "import { createApp } from './server.js'"
      pattern: "createApp"
    - from: "src/server/server.ts"
      to: "src/server/routes/api.ts"
      via: "app.route('/api/v1', apiRoutes)"
      pattern: "apiRoutes"
---

<objective>
Build the Hono server factory, CLI entry point, and /api/v1/summary API endpoint using TDD. This plan delivers the complete backend of Phase 3: server binds to 127.0.0.1, CSP headers block external requests, CLI flags parse correctly, and the summary endpoint returns aggregated data from the parser pipeline.

Purpose: The server and CLI form the backbone that Phase 4+ dashboard plans will build API routes on top of. Getting the security posture (127.0.0.1 + CSP) right here means all future phases inherit it automatically.
Output: src/server/{cli.ts,server.ts,routes/api.ts} + three test files + updated package.json (deps + bin) + updated tsup.config.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types the executor needs. Extracted from existing codebase. No exploration needed. -->

From src/index.ts:
```typescript
export async function parseAll(options?: ParseOptions): Promise<NormalizedEvent[]>
export function computeCosts(events: NormalizedEvent[]): CostEvent[]
export function applyPrivacyFilter(events: NormalizedEvent[]): NormalizedEvent[]
export type { NormalizedEvent, ParseOptions, CostEvent, EstimatedCost }
```

From src/parser/types.ts:
```typescript
export interface ParseOptions {
  dir?: string;   // override default discovery paths
  debug?: boolean;
}

export type NormalizedEvent = {
  uuid: string;
  type: string;
  timestamp: string;
  sessionId: string;
  tokens?: {
    input: number;
    output: number;
    cacheCreation: number;
    cacheRead: number;
    cacheCreation5m: number;
    cacheCreation1h: number;
  };
  model?: string;
  cwd?: string;
  // ... other optional fields (passthrough)
}
```

From src/cost/types.ts:
```typescript
export type EstimatedCost = number & { readonly [__brand]: 'EstimatedCost' }

export type CostEvent = NormalizedEvent & {
  costUsd: EstimatedCost;
  unknownModel?: true;
}
```
</interfaces>

<!-- Confirmed pitfalls from research — executor MUST read these before implementing -->
<!-- RESEARCH.md Pitfall 1: serveStatic uses process.cwd(), NOT import.meta.url.
     Use fileURLToPath(new URL('../../../web-dist', import.meta.url)) for absolute path. -->
<!-- RESEARCH.md Pitfall 3: Commander --no-open: declare ONLY the negative form.
     .option('--no-open', '...') creates opts.open=true by default; false when flag passed. -->
<!-- RESEARCH.md Pitfall 4: Register API routes BEFORE serveStatic. Static catch-all blocks API if first. -->
<!-- RESEARCH.md Pitfall 6: tsup dts with shebang. Use dts: { entry: ['src/index.ts'] } to limit
     declaration output to library entry only, avoiding potential shebang-in-dts errors. -->
</context>

<feature>
  <name>Hono server factory + CLI entry + /api/v1/summary</name>
  <files>
    src/server/__tests__/server.test.ts,
    src/server/__tests__/api.test.ts,
    src/server/__tests__/cli.test.ts,
    src/server/server.ts,
    src/server/routes/api.ts,
    src/server/cli.ts,
    package.json,
    tsup.config.ts
  </files>
  <behavior>
    RED phase — write failing tests first, then implement to make them pass.

    server.test.ts behaviors:
    - createApp({ events: [], costs: [] }) returns a Hono instance
    - GET /api/v1/summary on the app returns 200 (not 404 or HTML)
    - Response for GET /api/v1/summary has Content-Type: application/json
    - Hono app has Content-Security-Policy header on all responses
    - CSP header includes "default-src 'none'" substring
    - CSP header includes "connect-src 'self'" substring
    - CSP does NOT contain any external domain (http:// or https://)

    api.test.ts behaviors:
    - GET /api/v1/summary with empty state returns { totalCost: 0, totalTokens: { input:0, output:0, cacheCreation:0, cacheRead:0 }, eventCount: 0 }
    - GET /api/v1/summary with 2 CostEvents aggregates totalCost correctly (sum of costUsd)
    - GET /api/v1/summary eventCount equals number of CostEvents in state
    - totalTokens sums input/output/cacheCreation/cacheRead across all CostEvents

    cli.test.ts behaviors (option parsing only — mock open and serve):
    - Default opts.open is true when --no-open flag is not passed
    - opts.open is false when --no-open is passed
    - opts.port defaults to '3000'
    - opts.dir is undefined when --dir not passed
    - opts.dir is the given value when --dir /custom/path is passed
    NOTE: CLI integration (actual server start + browser open) is manual-only per research.
  </behavior>
  <implementation>
    Step 1 — Install backend deps (run in project root):
    ```bash
    npm install hono @hono/node-server commander open
    ```

    Step 2 — Update tsup.config.ts:
    - Add 'src/server/cli.ts' to entry array alongside existing 'src/index.ts'
    - Change dts to: dts: { entry: ['src/index.ts'] }  (limits .d.ts to library only)
    - Keep format: ['esm'], target: 'node22', clean: true, sourcemap: true

    Step 3 — Update package.json bin field:
    - Change "yclaude": "./dist/index.js"  →  "yclaude": "./dist/server/cli.js"

    Step 4 — Create src/server/routes/api.ts:
    - Export function apiRoutes(state: AppState): Hono (returns a sub-app with all /v1 routes)
    - GET /api/v1/summary: aggregate state.costs into { totalCost, totalTokens, eventCount }
      - totalCost: sum of all costUsd values (number — EstimatedCost serializes as plain number)
      - totalTokens: sum input/output/cacheCreation/cacheRead across events that have tokens
      - eventCount: state.costs.length
    - Stub routes (return empty shapes, no implementation):
      - GET /api/v1/events → { events: [] }
      - GET /api/v1/sessions → { sessions: [] }
    - These stubs enable Phase 4+ without breaking changes

    Step 5 — Create src/server/server.ts:
    Export interface AppState { events: NormalizedEvent[]; costs: CostEvent[] }
    Export function createApp(state: AppState): Hono
    - Apply secureHeaders() middleware globally with this exact CSP:
      contentSecurityPolicy: {
        defaultSrc: ["'none'"],
        scriptSrc: ["'self'"],
        styleSrc: ["'self'", "'unsafe-inline'"],
        imgSrc: ["'self'", "data:"],
        connectSrc: ["'self'"],
        fontSrc: ["'self'"],
        objectSrc: ["'none'"],
        frameAncestors: ["'none'"],
      }
    - Register API routes FIRST: app.route('/api/v1', apiRoutes(state))
    - THEN register static serving (address in plan 03 — leave a TODO comment for now)
    - Import pattern: import { secureHeaders } from 'hono/secure-headers'

    Step 6 — Create src/server/cli.ts:
    - First line MUST be: #!/usr/bin/env node
    - Import Commander, serve from @hono/node-server, open, parseAll, computeCosts, applyPrivacyFilter, createApp
    - Commander setup:
      .name('yclaude')
      .version('0.1.0', '-v, --version')
      .option('-d, --dir <path>', 'custom data directory')
      .option('-p, --port <number>', 'port number', '3000')
      .option('--no-open', 'do not open browser automatically')
      .parse()
    - Load data: const events = await parseAll({ dir: opts.dir }); const filtered = applyPrivacyFilter(events); const costs = computeCosts(filtered);
    - Create app: const app = createApp({ events: filtered, costs });
    - Start server: serve({ fetch: app.fetch, port, hostname: '127.0.0.1' }, () => { ... print startup output ... })
    - Terminal output (exact format from CONTEXT.md):
      console.log(`yclaude v0.1.0`)
      console.log(`Local: \x1b[1m${url}\x1b[0m`)
      if (opts.open) { console.log('\x1b[32m✓\x1b[0m Opening browser...'); open(url).catch(() => console.log(`→ Open ${url} in your browser`)); }
      else { console.log(`→ Open ${url} in your browser`); }
      console.log('Press Ctrl+C to stop')
    - All imports use .js extension (ESM requirement for TypeScript files in this project)
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write failing tests for server factory, API routes, and CLI options</name>
  <files>
    src/server/__tests__/server.test.ts,
    src/server/__tests__/api.test.ts,
    src/server/__tests__/cli.test.ts
  </files>
  <behavior>
    Write all tests listed in the feature behavior block above.
    Tests MUST fail at this point (no implementation exists yet).

    Test setup notes:
    - server.test.ts and api.test.ts: import { createApp } from '../server.js'; import { AppState } from '../server.js'
      Use Hono's built-in test helper: const res = await app.request('/api/v1/summary')
      Hono test helper: import { Hono } from 'hono'; use app.request() — no actual HTTP port needed.
    - cli.test.ts: test Commander option parsing in isolation.
      Create a helper parseCLIArgs(argv: string[]) that accepts raw argv and returns parsed opts.
      This avoids needing to import the full cli.ts (which has side effects).
      Pattern: Create a fresh Command instance inside the helper with the same option definitions.
      Test that parseCLIArgs(['--no-open']).open === false
      Test that parseCLIArgs([]).open === true
      Test that parseCLIArgs(['--port', '4000']).port === '4000'
      Test that parseCLIArgs(['--dir', '/tmp/data']).dir === '/tmp/data'
      Test that parseCLIArgs([]).dir === undefined

    Import paths use .js extension: import { createApp } from '../server.js'
    Run: npm test -- src/server/__tests__/ — ALL tests should FAIL (red).
  </behavior>
  <action>
    Install backend dependencies first (needed for imports to resolve):
    ```bash
    npm install hono @hono/node-server commander open
    ```

    Then create the three test files. Each test file imports from modules that do not yet exist.
    Vitest will report import errors or test failures — both count as RED.

    Commit: test(03-01): add failing tests for server, API routes, and CLI options
  </action>
  <verify>
    <automated>npm test -- src/server/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <done>Three test files exist, npm install completed successfully, tests run and fail (not skipped or missing)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement server factory, API routes, and CLI entry to pass all tests</name>
  <files>
    package.json,
    tsup.config.ts,
    src/server/server.ts,
    src/server/routes/api.ts,
    src/server/cli.ts
  </files>
  <behavior>
    Implement minimally to make all tests from Task 1 pass.
    Follow the implementation steps in the &lt;implementation&gt; block exactly.
    No gold-plating — only what makes tests green.
  </behavior>
  <action>
    1. Update tsup.config.ts — add CLI entry, restrict dts to library only:
    ```typescript
    import { defineConfig } from 'tsup';
    export default defineConfig({
      entry: ['src/index.ts', 'src/server/cli.ts'],
      format: ['esm'],
      target: 'node22',
      clean: true,
      dts: { entry: ['src/index.ts'] },
      sourcemap: true,
    });
    ```

    2. Update package.json bin field only (scripts stay unchanged — Plan 03 handles build scripts):
    "bin": { "yclaude": "./dist/server/cli.js" }

    3. Create src/server/routes/api.ts with apiRoutes factory and summary aggregation.

    4. Create src/server/server.ts with createApp factory, secureHeaders CSP middleware,
       API routes registered BEFORE static (leave a TODO comment for static serving — Plan 03).

    5. Create src/server/cli.ts with shebang, Commander options, startup sequence.
       NOTE: The CLI has side effects (program.parse()) so do NOT import it in tests.
       The cli.test.ts tests a standalone Commander instance, not cli.ts directly.

    Run npm test -- src/server/__tests__/ after each file to confirm progress.

    Commit when all tests pass: feat(03-01): implement server factory, API routes, and CLI entry
  </action>
  <verify>
    <automated>npm test -- src/server/__tests__/</automated>
  </verify>
  <done>All tests in src/server/__tests__/ pass. npm test exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Verify full test suite still passes after backend additions</name>
  <files></files>
  <action>
    Run the full test suite to confirm Phase 1 + Phase 2 tests are unaffected by the new server files.
    Also run typecheck on the backend (web/ typecheck happens in Plan 02).

    ```bash
    npm test
    npm run typecheck
    ```

    If typecheck finds issues with the shebang in cli.ts dts generation, confirm tsup.config.ts
    has dts: { entry: ['src/index.ts'] } (not dts: true). This scopes .d.ts output to the
    library entry only, bypassing the shebang-in-dts issue described in RESEARCH.md Pitfall 6.

    Commit if only minor fixes needed: refactor(03-01): fix typecheck issues in server files
  </action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>npm test passes (all phases), npm run typecheck exits 0 with no errors</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. npm test passes with zero failures across all test files
2. npm run typecheck exits 0
3. src/server/__tests__/ contains three test files with passing tests
4. src/server/cli.ts has #!/usr/bin/env node as line 1
5. src/server/server.ts exports createApp and AppState
6. package.json bin.yclaude points to ./dist/server/cli.js
7. tsup.config.ts entry array includes 'src/server/cli.ts'
</verification>

<success_criteria>
- All three test files pass: CLI-02 (flag parsing), CORE-04 (127.0.0.1 binding + CSP headers), CORE-04 (/api/v1/summary shape)
- Full test suite still green (no Phase 1/2 regressions)
- Backend server and CLI source files exist with correct structure per research patterns
- package.json and tsup.config.ts updated to support CLI binary output
</success_criteria>

<output>
After completion, create .planning/phases/03-server-cli-app-shell/03-01-SUMMARY.md
</output>
